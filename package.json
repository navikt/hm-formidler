{
  "name": "hm-formidler",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.0.0",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "dev": "npm run --prefix client dev",
    "build": "concurrently --kill-others-on-fail \"npm run --prefix client build\""
  }
}
