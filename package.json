{
  "name": "hm-formidler",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "concurrently": "^7.1.0",
    "cross-env": "^7.0.3",
    "prettier": "^2.6.2"
  },
  "scripts": {
    "dev": "yarn --cwd ./client dev",
    "build": "concurrently --kill-others-on-fail \"yarn --cwd ./client build\" \"yarn --cwd ./server build\""
  },
  "dependencies": {}
}
