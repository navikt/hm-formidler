{
  "name": "hm-formidler",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "concurrently": "^7.1.0",
    "cross-env": "^7.0.3",
    "prettier": "^2.6.2"
  },
  "scripts": {
    "dev": "npm run --prefix client dev",
    "build": "concurrently --kill-others-on-fail \"npm run --prefix client build\" \"npm run --prefix server build\""
  },
  "dependencies": {
    "@navikt/ds-css": "^7.28.1",
    "@navikt/ds-react": "^7.28.1"
  }
}
